<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.springoauth.dao.UserMapper">

    <resultMap type="com.springoauth.entity.UserEntity" id="carTypeMap">
        <result property="id" column="id"/>
        <result property="username" column="username"/>
        <result property="password" column="password"/>
        <result property="status" column="status"/>
    </resultMap>

    <select id="getUserByName" resultType="com.springoauth.entity.UserEntity">
        SELECT id, `username`, `password`, status FROM sys_user WHERE username = #{username}
    </select>

</mapper>